{
  "name": "docsy-starter",
  "scripts": {
    "_build": "npm run _hugo-dev --",
    "_check:format": "prettier --check .",
    "_check:links--warn": "npm run _check:links || (echo; echo 'WARNING: see link-checker output for issues.'; echo)",
    "_check:links:internal": "npm run _check:links -- HTMLTEST_ARGS='--skip-external'",
    "_check:links": "make --keep-going check-links",
    "_commit:public": "HASH=$(git rev-parse --short main); cd public && git add -A && git commit --allow-empty -m \"Site at $HASH\"",
    "_diff:check": "git diff --name-only --exit-code",
    "_filename-error": "echo 'ERROR: the following files violate naming conventions; fix using: `npm run fix:filenames`'; echo; npm run -s _ls-bad-filenames; exit 1",
    "_filenames-to-kebab-case": "find assets content -name '*_*' ! -name '[_.]*' -exec sh -c 'mv \"$1\" \"${1//_/-}\"' _ {} \\;",
    "_hugo-dev": "npm run _hugo -- -e dev -DFE",
    "_hugo": "hugo --cleanDestinationDir",
    "_ls-bad-filenames": "find assets content -name '*_*' ! -name '[_.]*'",
    "_serve": "npm run _hugo-dev -- serve --minify --disableFastRender --renderToMemory",
    "_rebuild:hugo": "npm rebuild hugo-extended --ignore-scripts=false",
    "_build:preview": "npm run _hugo-dev -- --minify --baseURL \"${DEPLOY_PRIME_URL:-http://localhost}\"",
    "_build:production": "npm run _hugo -- --minify",
    "build:preview": "npm run _build:preview && npm run _check:links--warn",
    "build:production": "npm run _build:production && npm run _check:links--warn",
    "build": "npm run _build --",
    "check:filenames": "test -z \"$(npm run -s _ls-bad-filenames)\" || npm run -s _filename-error",
    "check:format": "npm run _check:format || (echo '[help] Run: npm run fix:format'; exit 1)",
    "check:links:internal": "HTMLTEST_ARGS='--skip-external' npm run _check:links",
    "check:links": "npm run build && npm run _check:links",
    "check:spelling": "cspell --no-progress -c .cspell.yml content data 'layouts/**/*.md'",
    "clean": "rm -Rf public/",
    "diff:check": "npm run _diff:check || (echo; echo 'WARNING: the files above have not been committed'; echo)",
    "fix:filenames": "npm run _filenames-to-kebab-case",
    "fix:format": "npm run _check:format -- --write",
    "install:safe": "npm ci --ignore-scripts --no-audit --no-fund && npm run _rebuild:hugo",
    "is:clean": "bash -c 'o=$(git status --porcelain -uall); echo \"$o\"; [ -z \"$o\" ]'",
    "_netlify:prepare": "npm run -s is:clean && npm run install:safe && npm run -s is:clean",
    "make:public": "git init public",
    "random:text": "node scripts/random-text.js",
    "seq": "bash -c 'for cmd in \"$@\"; do npm run $cmd || exit 1; done' - ",
    "serve": "npm run _serve",
    "test": "npm list @docsy/theme && npm run check:format && npm run check:links",
    "update:docsy:local": "npm install -D file:../docsy/docsy/theme && npm list @docsy/theme",
    "update:docsy": "echo 'INFO: set VERSION to desired Docsy version' && npm install -D @docsy/theme@${VERSION:-latest} && npm list @docsy/theme",
    "update:hugo": "npm install --save-exact -D hugo-extended@latest",
    "update:packages": "npm exec --no -- npm-check-updates --dep 'prod,dev,optional,peer' -u"
  },
  "devDependencies": {
    "@docsy/theme": "^0.16.0",
    "cspell": "^10.0.0",
    "hugo-extended": "0.164.0",
    "prettier": "^3.8.3"
  },
  "optionalDependencies": {
    "npm-check-updates": "23.0.2"
  },
  "enginesComment": "Ensure that engines.node value stays consistent with the project's .nvmrc; the npm floor is where allowScripts support landed (silently ignored on older npm)",
  "engines": {
    "node": ">=24",
    "npm": ">=11.18.0"
  },
  "allowScripts": {
    "hugo-extended@0.164.0": true
  },
  "private": true,
  "prettier": {
    "proseWrap": "always",
    "singleQuote": true
  },
  "spelling": "cSpell:ignore docsy hugo HTMLTEST netlify -"
}
