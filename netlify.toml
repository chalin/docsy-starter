[build]
publish = "public"
command = "npm run _netlify:prepare && npm run seq -- build:preview diff:check"

[build.environment]
# Constrain Netlify's automatic npm install to resolution only: no tree
# writes, no bin linking; install:safe does the real install. Netlify offers
# no way to skip the automatic install, so if NPM_FLAGS ever stops being
# honored, it runs for real: the committed ignore-scripts still blocks its
# scripts, and install:safe's lock-exact npm ci replaces its tree. The
# is:clean bracket flags Git-visible writes only (node_modules is ignored).
NPM_FLAGS = "--dry-run --ignore-scripts"

[context.production]
command = "npm run _netlify:prepare && npm run seq -- build:production diff:check"

[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    X-Frame-Options = "SAMEORIGIN"
    Content-Security-Policy = "frame-ancestors 'self';"

# cSpell:ignore nosniff sameorigin
